get:
  tags:
    - Network
  summary: Get wan network connections
  responses:
    200:
      description: Network state retrieved successfully
      schema:
        type: array
        items:
          $ref: '#/definitions/ConnectionStateWAN'