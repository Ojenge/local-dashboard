get:
  tags:
    - Network
  summary: Get ethernet network connections
  responses:
    200:
      description: Network state retrieved successfully
      schema:
        type: array
        items:
          $ref: '#/definitions/ConnectionStateEthernet'