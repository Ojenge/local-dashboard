parameters:
  - $ref: '#/parameters/connectionId'
patch:
  tags:
    - Network
  summary: Updates network connection.
  parameters:
    - name: changes
      in: body
      required: true
      schema:
        $ref: '#/definitions/ChangeNetworkRequest'
  responses:
    200:
      description: Network was updated successfully
      schema:
        $ref: '#/definitions/ConnectionState'
    400:
      description: The network update failed
      schema:
        $ref: '#/definitions/Error'