type: object
properties:
  id:
    type: string
    description: The unique ID of the device
  battery:
    description: The battery status of the unit
    $ref: '#/definitions/BatteryStatus'
  storage:
    description: The storage status of the unit
    $ref: '#/definitions/StorageStatus'
  mode:
    $ref: '#/definitions/DeviceMode'
  power:
    $ref: '#/definitions/PowerSettings'
  network:
    $ref: '#/definitions/SystemNetworkStatus'