type: object
properties:
  configuration:
    type: object
    properties:
      pin:
        type: string
        pattern: '\d{4}|'
        description: PIN to use to unlock the SIM (When SIM is locked)
        example: "1234"
      network:
        $ref: '#/definitions/WanIPConfig'
        optional: true