type: object
properties:
  configuration:
    type: object
    properties:
      dhcp_enabled:
        type: boolean
        description: Use DHCP to acquire IP configuration
        example: true
      network:
        $ref: '#/definitions/EthernetIPConfig'
